// Phase 1: minimal schema; will add User and full entities in Step 2.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Placeholder {
  id String @id @default(cuid())
}
