{
  "name": "@expense-management/db",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "prisma generate && tsc",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "seed": "tsx seed.ts",
    "seed:preview": "tsx seed.ts --preview",
    "sync:institutions": "tsx scripts/sync-institutions.ts",
    "lint": "echo no lint yet",
    "typecheck": "prisma generate && tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "prisma": "^6.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@prisma/client": "6.19.2",
    "better-auth": "^1.4.18"
  }
}
