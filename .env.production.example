# Production environment template (DO)
# Copy to .env.production on the server and fill real values.

NODE_ENV=production
PORT=4000

# Public app origin(s)
CORS_ORIGINS="https://your-domain.com"
BETTER_AUTH_URL="https://your-domain.com"
BETTER_AUTH_SECRET="replace-with-a-32+-char-random-secret"

# Postgres credentials used by the DB container
POSTGRES_DB=expense_management
POSTGRES_USER=expense_management
POSTGRES_PASSWORD="replace-with-strong-db-password"

# API uses this to connect to internal DB service
DATABASE_URL="postgresql://expense_management:replace-with-strong-db-password@postgres:5432/expense_management?schema=public"

# Web build arg. Keep empty when nginx and API are same origin.
# If you host API on a different domain, set full URL like https://api.example.com
VITE_API_URL=""
