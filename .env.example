# Copy to .env and fill in. Required for DB and API.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/expense_management?schema=public"

# Optional: API base URL for the web app (defaults to http://localhost:4000)
# VITE_API_URL=http://localhost:4000
